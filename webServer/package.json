{
  "name": "webserver",
  "version": "1.0.0",
  "description": "Creating the package.json using npm and node.js",
  "main": "serverURL.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shiva Chouhan",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^3.1.9"
  }
}
